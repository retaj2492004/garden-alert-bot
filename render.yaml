services:
  - type: web
    name: garden-alert-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        value: "توكن_البوت"
      - key: CHAT_ID
        value: "123456789"
    plan: free
